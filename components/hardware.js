const Hardware = ({}) => (
  <section className="hardware">
    <h2>Support for a variety of hardware</h2>

    <style jsx>{`
      section {
        align-items: center;
      }
    `}</style>
  </section>
);

export default Hardware;
